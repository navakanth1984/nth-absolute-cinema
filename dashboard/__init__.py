"""Sprint 2A Director Studio - FastAPI server + static UI. Imports only
`from nac import Studio` - never engine.* directly, same boundary discipline as
the CLI and the Agent OS bridge (agent_os/filmmaking/nac_bridge.py)."""
